import { Metadata, Viewport } from 'next';

export const metadata: Metadata = {
  title: 'Page Not Found - FinanceTodoBot',
  description: 'The page you are looking for does not exist',
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};