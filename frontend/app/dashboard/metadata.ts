import { Metadata, Viewport } from 'next';

export const metadata: Metadata = {
  title: 'Dashboard - FinanceTodoBot',
  description: 'Your financial dashboard and task management center',
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};