import { Metadata, Viewport } from 'next';

export const metadata: Metadata = {
  title: 'Sign Up - FinanceTodoBot',
  description: 'Create your financial task management account',
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};